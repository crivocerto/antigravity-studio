# Tarefas: Spec 013

- `[ ]` 13.1 Criar a lógica de parsing JSON dinâmico dentro de `src/data/posts.ts` (na função `mapPost`).
- `[ ]` 13.2 Configurar fallback das colunas: se for JSON, pegar os dados do JSON; se não for, continuar usando as colunas diretas como `dbPost.hero_image`.
- `[ ]` 13.3 Fazer o parser de links de afiliados, mapeando `price_amazon` e `price_ml` em objetos padronizados compatíveis com `affiliateLinks`.
